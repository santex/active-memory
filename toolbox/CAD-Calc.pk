#!/usr/bin/perl

use_ok('Data::Dumper');

use Test::More qw(
	no_plan
	);

use_ok('CAD::Calc');

SKIP: {
	my $helper = 'Math::Geometry::Planar::Offset';
	my $ok = eval("require $helper;"); 
	$ok or skip("could not load $helper");
@gons = CAD::Calc::offset([ [0,0], [1,0], [1,1], [0,1] ], 0.25);
	print Dumper \@gons;
}
